```bash
    # variables d'environnements [.env]
    # à l'interieur de rechecdi et un autre a 
    # coté du docker-compose
    POSTGRES_USER=
    POSTGRES_PASSWORD=
    POSTGRES_DB=
    POSTGRES_PORT=
    APP_PORT=
```